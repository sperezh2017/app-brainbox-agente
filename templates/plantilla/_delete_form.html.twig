<form method="post" action="{{ path('app_plantilla_delete', {'id': pro_transaccion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pro_transaccion.id) }}">
    <button class="btn">Delete</button>
</form>
