{% extends 'base.html.twig' %}

{% block title %}ProTransaccion{% endblock %}

{% block body %}
    <h1>ProTransaccion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pro_transaccion.id }}</td>
            </tr>
            <tr>
                <th>ProcesoNombre</th>
                <td>{{ pro_transaccion.procesoNombre }}</td>
            </tr>
            <tr>
                <th>FechaInicio</th>
                <td>{{ pro_transaccion.fechaInicio ? pro_transaccion.fechaInicio|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FechaVence</th>
                <td>{{ pro_transaccion.FechaVence ? pro_transaccion.FechaVence|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plantilla_index') }}">back to list</a>

    <a href="{{ path('app_plantilla_edit', {'id': pro_transaccion.id}) }}">edit</a>

    {{ include('plantilla/_delete_form.html.twig') }}
{% endblock %}
