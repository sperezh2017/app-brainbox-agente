<form method="post" action="{{ path('app_cat_sub_procesos_delete', {'id': cat_sub_proceso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cat_sub_proceso.id) }}">
    <button class="btn">Delete</button>
</form>
