{% extends 'base.html.twig' %}

{% block title %}CatSubProcesos{% endblock %}

{% block body %}
    <h1>CatSubProcesos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_sub_proceso.id }}</td>
            </tr>
            <tr>
                <th>Subproceso</th>
                <td>{{ cat_sub_proceso.subproceso }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cat_sub_procesos_index') }}">back to list</a>

    <a href="{{ path('app_cat_sub_procesos_edit', {'id': cat_sub_proceso.id}) }}">edit</a>

    {{ include('cat_sub_procesos/_delete_form.html.twig') }}
{% endblock %}
